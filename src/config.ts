/**
 * Retrieve App configuration
 */

import { LANGUAGE_ENGLISH, LANGUAGE_SPANISH } from '@modules/admin/constants';

export const config = {
  language: LANGUAGE_SPANISH,
  URL_CLIENT: process.env.URL_CLIENT,
};
